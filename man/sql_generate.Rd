% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transpile.R
\name{sql_generate}
\alias{sql_generate}
\title{Generate SQL from a parsed AST}
\usage{
sql_generate(ast, dialect = "generic")
}
\arguments{
\item{ast}{A \code{polyglot_ast} object from \code{\link[=sql_parse]{sql_parse()}} (or
\code{\link[=sql_annotate_types]{sql_annotate_types()}}).}

\item{dialect}{Target dialect for rendering.}
}
\value{
A character vector with one element per statement in the AST.
}
\description{
Renders a \code{\link[=sql_parse]{sql_parse()}} result back into SQL text using the target
dialect's syntax rules (keywords, quoting, literals). Note this is plain
\emph{generation}: unlike \code{\link[=sql_transpile]{sql_transpile()}}, it does not apply cross-dialect
function rewrites (e.g. \code{IFNULL} is not converted to \code{COALESCE}). Use it
to render programmatically-built or modified ASTs; use \code{\link[=sql_transpile]{sql_transpile()}}
for full dialect translation.
}
\examples{
ast <- sql_parse("SELECT a, b FROM t WHERE x = 1")
sql_generate(ast, dialect = "postgres")
}
