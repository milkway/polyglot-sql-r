% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transpile.R
\name{sql_format}
\alias{sql_format}
\title{Format (pretty-print) SQL}
\usage{
sql_format(
  sql,
  dialect = "generic",
  max_input_bytes = NULL,
  max_tokens = NULL,
  max_ast_nodes = NULL,
  max_set_op_chain = NULL
)
}
\arguments{
\item{sql}{A single character string with one or more SQL statements
(separated by \verb{;}).}

\item{dialect}{Dialect used for parsing and rendering.}

\item{max_input_bytes, max_tokens, max_ast_nodes, max_set_op_chain}{Complexity
guard limits. \code{NULL} (default) uses the upstream defaults (16 MiB input,
1e6 tokens, 1e6 AST nodes, 256 chained set operations); \code{Inf} disables a
guard; a positive number sets an explicit limit.}
}
\value{
A character vector with one formatted statement per input statement.
}
\description{
Parses and re-renders SQL as canonically indented statements. Complexity
guards protect against pathological inputs; exceeding a guard raises a
\code{polyglot_guard_error}.
}
\examples{
cat(sql_format("select a,b from t where x=1 and y=2"))
}
