% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{sql_lineage}
\alias{sql_lineage}
\title{Column-level lineage}
\usage{
sql_lineage(sql, dialect = "generic", schema = NULL, column = NULL)
}
\arguments{
\item{sql}{A single character string with one or more SQL statements
(separated by \verb{;}).}

\item{dialect}{Dialect used for parsing.}

\item{schema}{Optional schema specification (see \code{\link[=as_polyglot_schema]{as_polyglot_schema()}});
improves resolution of unqualified or ambiguous columns.}

\item{column}{Optional single column name. By default, lineage is computed
for every output column of the query.}
}
\value{
A \code{polyglot_lineage} object: a list with one entry per column, each
containing
\itemize{
\item \code{column} — the output column name;
\item \code{sources} — character vector of source tables feeding this column;
\item \code{tree} — the lineage graph as nested lists with fields \code{name},
\code{source_name}, \code{source_kind} (\code{"Table"}, \code{"Cte"}, \code{"DerivedTable"},
...), \code{expression} (the SQL of the node's expression) and \code{downstream}.
}
}
\description{
Traces each output column of a query back to the tables and expressions it
is derived from, following CTEs, subqueries and set operations.
}
\examples{
sql_lineage("SELECT a + b AS total FROM t")
sql_lineage(
  "WITH base AS (SELECT id, amount FROM payments)
   SELECT id, amount * 2 AS doubled FROM base"
)
}
