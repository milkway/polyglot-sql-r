% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{sql_diff}
\alias{sql_diff}
\title{Structural diff between two SQL statements}
\usage{
sql_diff(sql_from, sql_to, dialect = "generic", delta_only = TRUE)
}
\arguments{
\item{sql_from, sql_to}{Single SQL statements to compare.}

\item{dialect}{Dialect used for parsing.}

\item{delta_only}{If \code{TRUE} (default), omit unchanged (\code{"keep"}) nodes.}
}
\value{
A data frame with columns \code{op} (\code{"insert"}, \code{"remove"}, \code{"move"},
\code{"update"}, \code{"keep"}), \code{expression} (SQL of the affected node) and
\code{target} (for updates, the new SQL).
}
\description{
Compares the ASTs of two statements and reports inserted, removed, moved
and updated nodes.
}
\examples{
sql_diff("SELECT a FROM t", "SELECT a, b FROM t WHERE a > 1")
}
