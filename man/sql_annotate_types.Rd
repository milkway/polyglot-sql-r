% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{sql_annotate_types}
\alias{sql_annotate_types}
\title{Annotate a query with inferred data types}
\usage{
sql_annotate_types(sql, dialect = "generic", schema = NULL)
}
\arguments{
\item{sql}{A single character string with one or more SQL statements
(separated by \verb{;}).}

\item{dialect}{Dialect used for parsing.}

\item{schema}{Optional schema specification (see \code{\link[=as_polyglot_schema]{as_polyglot_schema()}});
improves resolution of unqualified or ambiguous columns.}
}
\value{
A \code{polyglot_ast} object whose nodes carry an \code{inferred_type} field
where a type could be determined. Pass it to \code{\link[=sql_generate]{sql_generate()}} to render,
or inspect \verb{$statements} directly.
}
\description{
Runs upstream type inference over the AST. With a \code{schema}, column
references resolve to their declared types; without one, only types that
can be inferred from literals, casts and function signatures are filled.
}
\examples{
ast <- sql_annotate_types(
  "SELECT id + 1 AS next_id FROM t",
  schema = list(t = c(id = "INT"))
)
# the Add node now carries inferred_type INT
}
