% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse.R
\name{sql_tokenize}
\alias{sql_tokenize}
\title{Tokenize SQL}
\usage{
sql_tokenize(sql, dialect = "generic")
}
\arguments{
\item{sql}{A single character string with one or more SQL statements
(separated by \verb{;}).}

\item{dialect}{Dialect used for parsing.}
}
\value{
A data frame with class \code{polyglot_tokens} and one row per token:
\code{type} (token type, e.g. \code{"Select"}, \code{"Identifier"}, \code{"Number"}),
\code{text} (raw token text), \code{line} and \code{column} (1-based position),
\code{start} and \code{end} (byte offsets, end exclusive).
}
\description{
Splits SQL into lexical tokens using the tokenizer of the given dialect.
}
\examples{
sql_tokenize("SELECT a FROM t")
}
