% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transpile.R
\name{sql_transpile}
\alias{sql_transpile}
\title{Translate SQL between dialects}
\usage{
sql_transpile(
  sql,
  from,
  to,
  pretty = FALSE,
  unsupported = c("raise", "warn", "ignore")
)
}
\arguments{
\item{sql}{A single character string with one or more SQL statements
(separated by \verb{;}).}

\item{from}{Source dialect name (see \code{\link[=sql_dialects]{sql_dialects()}}).}

\item{to}{Target dialect name (see \code{\link[=sql_dialects]{sql_dialects()}}).}

\item{pretty}{If \code{TRUE}, pretty-print the output.}

\item{unsupported}{How to handle constructs that cannot be represented in
the target dialect: \code{"raise"} (default) throws a
\code{polyglot_transpile_error}; \code{"warn"} and \code{"ignore"} continue and return
the closest supported translation (with \code{"warn"}, upstream collects
diagnostics but still returns a result).}
}
\value{
A character vector with one element per input statement.
}
\description{
Parses \code{sql} with the \code{from} dialect and regenerates it in the \code{to}
dialect, rewriting functions, quoting, and constructs as needed
(e.g. MySQL \code{IFNULL()} becomes PostgreSQL \code{COALESCE()}).
}
\section{Semantic limitations}{

Transpilation is syntactic and best-effort: identical syntax can still
behave differently across engines (implicit casts, collations, \code{NULL}
ordering, integer division, time zone handling...). Always test the
translated SQL against the target database before using it in production.
}

\examples{
sql_transpile("SELECT IFNULL(a, b) FROM t", from = "mysql", to = "postgres")
sql_transpile(
  "SELECT DATE_TRUNC('month', created_at) FROM events",
  from = "postgres", to = "duckdb"
)
}
\seealso{
\code{\link[=sql_format]{sql_format()}}, \code{\link[=sql_parse]{sql_parse()}}, \code{\link[=sql_validate]{sql_validate()}}
}
