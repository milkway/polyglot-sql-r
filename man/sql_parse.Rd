% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse.R
\name{sql_parse}
\alias{sql_parse}
\title{Parse SQL into an abstract syntax tree}
\usage{
sql_parse(sql, dialect = "generic")
}
\arguments{
\item{sql}{A single character string with one or more SQL statements
(separated by \verb{;}).}

\item{dialect}{Dialect used for parsing.}
}
\value{
A \code{polyglot_ast} object: a list with elements
\itemize{
\item \code{statements} — a list with one nested-list AST per statement;
\item \code{sql} — the input SQL;
\item \code{dialect} — the dialect used.
}

Each AST node is a named list; the name of the outer element gives the
node kind (e.g. \code{"select"}). The structure follows the upstream
\code{polyglot-sql} JSON AST format and round-trips through \code{\link[=sql_generate]{sql_generate()}}.
}
\description{
Parse SQL into an abstract syntax tree
}
\examples{
ast <- sql_parse("SELECT a, b FROM t WHERE x = 1")
ast
names(ast$statements[[1]])
}
