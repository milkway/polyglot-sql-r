% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata.R
\name{sql_dialects}
\alias{sql_dialects}
\title{List supported SQL dialects}
\usage{
sql_dialects(full = FALSE)
}
\arguments{
\item{full}{If \code{FALSE} (default), return a character vector of canonical
dialect names. If \code{TRUE}, return a data frame with columns \code{name},
\code{aliases} (comma-separated accepted aliases) and \code{description}.}
}
\value{
A character vector, or a data frame when \code{full = TRUE}.
}
\description{
List supported SQL dialects
}
\details{
Every function that takes a \code{dialect}, \code{from} or \code{to} argument
accepts both the canonical names and the aliases (e.g. \code{"postgresql"}
for \code{"postgres"}, \code{"mssql"} or \code{"sqlserver"} for \code{"tsql"}).
}
\examples{
sql_dialects()
head(sql_dialects(full = TRUE))
}
