% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse.R
\name{sql_validate}
\alias{sql_validate}
\title{Validate SQL}
\usage{
sql_validate(sql, dialect = "generic", ...)
}
\arguments{
\item{sql}{A single character string with one or more SQL statements
(separated by \verb{;}).}

\item{dialect}{Dialect used for parsing.}

\item{...}{Additional validation options:
\itemize{
\item \code{strict_syntax} — reject non-canonical syntax the parser would accept
for compatibility (e.g. trailing commas before \code{FROM}); default \code{FALSE}.
\item \code{semantic} — report query-quality warnings (\code{W001}–\code{W004}, e.g.
\verb{SELECT *} mixed with explicit columns); default \code{FALSE}.
\item \code{schema} — a schema specification (see \code{\link[=as_polyglot_schema]{as_polyglot_schema()}});
enables unknown-table/column, type and reference checks.
\item \code{error} — if \code{TRUE}, raise a \code{polyglot_validation_error} instead of
returning an invalid result; default \code{FALSE}.
}}
}
\value{
A \code{polyglot_validation} object: a list with \code{valid} (logical) and
\code{errors} (data frame with columns \code{severity}, \code{code}, \code{message}, \code{line},
\code{column}).
}
\description{
Checks that SQL parses in the given dialect and, optionally, applies
stricter syntax rules, semantic lint warnings, and schema-aware checks.
}
\examples{
sql_validate("SELECT a FROM t")
sql_validate("SELECT FROM WHERE")
v <- sql_validate("SELECT a, FROM t", strict_syntax = TRUE)
v$valid
}
