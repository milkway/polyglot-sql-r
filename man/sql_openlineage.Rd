% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{sql_openlineage}
\alias{sql_openlineage}
\title{OpenLineage column-lineage facet}
\usage{
sql_openlineage(
  sql,
  dialect = "generic",
  schema = NULL,
  namespace = NULL,
  job_name = NULL
)
}
\arguments{
\item{sql}{A single character string with one or more SQL statements
(separated by \verb{;}).}

\item{dialect}{Dialect used for parsing.}

\item{schema}{Optional schema specification (see \code{\link[=as_polyglot_schema]{as_polyglot_schema()}});
improves resolution of unqualified or ambiguous columns.}

\item{namespace}{Optional dataset namespace applied to inferred datasets.}

\item{job_name}{Optional job name recorded in the facet.}
}
\value{
A list with elements \code{facet} (the OpenLineage \code{columnLineage}
facet), \code{inputs}, \code{outputs} (dataset descriptors) and \code{warnings}.
}
\description{
Produces an \href{https://openlineage.io/}{OpenLineage}-compatible
\code{columnLineage} facet plus inferred input/output datasets for a SQL
statement, for integration with data catalogs and lineage backends.
}
\examples{
ol <- sql_openlineage(
  "INSERT INTO reports SELECT id, total FROM sales",
  namespace = "warehouse"
)
names(ol)
}
