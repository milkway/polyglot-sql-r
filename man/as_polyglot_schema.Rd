% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{as_polyglot_schema}
\alias{as_polyglot_schema}
\title{Specify a table schema for schema-aware operations}
\usage{
as_polyglot_schema(schema)
}
\arguments{
\item{schema}{A schema specification (named list as described above), or
\code{NULL} for no schema.}
}
\value{
A JSON string in the upstream \code{ValidationSchema} format, or \code{""}
when \code{schema} is \code{NULL}. Mostly used internally; exported for advanced
users who want to inspect the generated payload.
}
\description{
Several polyglotSQL functions (\code{\link[=sql_validate]{sql_validate()}}, \code{\link[=sql_lineage]{sql_lineage()}},
\code{\link[=sql_analyze]{sql_analyze()}}, \code{\link[=sql_optimize]{sql_optimize()}}, \code{\link[=sql_annotate_types]{sql_annotate_types()}},
\code{\link[=sql_openlineage]{sql_openlineage()}}) accept an optional \code{schema} argument describing the
tables referenced by the query. A schema enables column qualification,
type inference, and existence checks.
}
\details{
A schema is a named list with one entry per table. Each entry is either:
\itemize{
\item a \emph{named} character vector mapping column names to SQL types, e.g.
\code{c(id = "INT", name = "TEXT")};
\item an \emph{unnamed} character vector of column names (types unknown), e.g.
\code{c("id", "name")}.
}
}
\examples{
as_polyglot_schema(list(
  orders = c(o_id = "INT", o_total = "DECIMAL(10,2)"),
  users = c("id", "name")
))
}
