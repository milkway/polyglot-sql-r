% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{sql_analyze}
\alias{sql_analyze}
\title{Structural query analysis}
\usage{
sql_analyze(sql, dialect = "generic", schema = NULL)
}
\arguments{
\item{sql}{A single character string with one or more SQL statements
(separated by \verb{;}).}

\item{dialect}{Dialect used for parsing.}

\item{schema}{Optional schema specification (see \code{\link[=as_polyglot_schema]{as_polyglot_schema()}});
improves resolution of unqualified or ambiguous columns.}
}
\value{
A \code{polyglot_analysis} object — a list with (among others):
\itemize{
\item \code{shape} — query shape (e.g. \code{"select"}, \code{"setOperation"});
\item \code{projections} — list of per-output-column facts (name, transform kind,
upstream column references, type hints when a \code{schema} is given);
\item \code{relations} — list of referenced relations with kind and alias;
\item \code{ctes}, \code{cteFacts} — CTE names and per-CTE facts;
\item \code{setOperations}, \code{starProjections} — when present.
}
}
\description{
Extracts compact facts about a query: its shape, output projections,
referenced relations, CTEs, set operations and star-projections.
}
\examples{
a <- sql_analyze("WITH x AS (SELECT id FROM t) SELECT x.id, 2 AS two FROM x")
a$shape
vapply(a$projections, function(p) p$name, character(1))
}
